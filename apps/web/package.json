{
  "name": "@commerce-copilot/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.json --noEmit --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.workspace.ts --project web-ui apps/web"
  },
  "dependencies": {
    "@commerce-copilot/contracts": "workspace:*",
    "@commerce-copilot/ui": "workspace:*"
  }
}

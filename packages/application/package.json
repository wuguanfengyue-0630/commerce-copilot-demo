{
  "name": "@commerce-copilot/application",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.workspace.ts --project node packages/application"
  },
  "dependencies": {
    "@commerce-copilot/contracts": "workspace:*",
    "@commerce-copilot/domain": "workspace:*"
  }
}

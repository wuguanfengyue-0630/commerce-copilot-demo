{
  "name": "@commerce-copilot/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.workspace.ts --project node packages/agent"
  },
  "dependencies": {
    "@commerce-copilot/application": "workspace:*",
    "@commerce-copilot/contracts": "workspace:*"
  }
}

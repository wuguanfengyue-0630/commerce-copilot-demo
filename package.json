{
  "name": "commerce-copilot",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "24.14.1",
    "pnpm": "11.7.0"
  },
  "scripts": {
    "dev": "pnpm --filter @commerce-copilot/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "test": "vitest run --config vitest.workspace.ts",
    "test:watch": "vitest --config vitest.workspace.ts",
    "test:e2e": "playwright test",
    "verify": "pnpm verify:workspace && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "verify:workspace": "node --test scripts/verify-workspace.test.mjs && node scripts/verify-workspace.mjs && node scripts/verify-package-imports.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@playwright/test": "1.61.1",
    "@types/node": "24.13.3",
    "@vitest/coverage-v8": "4.1.10",
    "jsdom": "29.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
